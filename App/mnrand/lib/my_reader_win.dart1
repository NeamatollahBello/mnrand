import 'package:flutter/material.dart';

class ReaderWidget extends StatelessWidget {
  final Function(dynamic p) onScan;

  const ReaderWidget({super.key, required this.onScan});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
